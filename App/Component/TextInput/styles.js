import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    inputStyle : {
        width : '100%',
        height : 60,
        borderRadius: 12,
        borderWidth : 1,
        borderColor: '#000',
        paddingLeft: 15,
        fontSize : 16
    }
})

export default styles